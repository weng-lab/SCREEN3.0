/**
 * EntityHeader is the entry point — it picks the right header for an entity type. The per-entity
 * headers, the shared layout, and the link buttons are internal to this folder.
 */
export * from "./EntityHeader";
