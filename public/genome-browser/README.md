# Cytobands

`hg38.cytoBand.txt` is copied from `genomebrowser/apps/standalone/public/data/hg38.cytoBand.txt`
for local, API-independent human chromosome ideograms. It uses UCSC's five-column cytoband format.
Mouse ideograms currently retain SCREEN's existing `cytoband(assembly: "mm10")` API query.
